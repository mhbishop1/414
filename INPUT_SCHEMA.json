{
    "title": "Group Research Orchestrator",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "apiToken": {
            "title": "Apify API Token",
            "type": "string",
            "description": "Your personal API token (Settings > Integrations). Required to run other Actors.",
            "editor": "textfield",
            "isSecret": true
        },
        "maxItems": {
            "title": "Max Items per Platform",
            "type": "integer",
            "description": "Number of results to pull from each site.",
            "default": 10
        }
    },
    "required": ["apiToken"]
}
